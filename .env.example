# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=personalblog
DB_USER=postgres
DB_PASSWORD=change-this-password-in-production
# Optional: use a full JDBC URL instead of DB_HOST/DB_PORT/DB_NAME
# DB_URL=jdbc:postgresql://localhost:5432/personalblog

# JWT Configuration
JWT_SECRET=change-this-secret-in-production-use-long-random-string
JWT_ISSUER=personalblog
JWT_AUDIENCE=personalblog-users

# Backend Configuration
BACKEND_PORT=9191

